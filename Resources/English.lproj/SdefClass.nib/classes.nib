{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {CLASS = SKImageAndTextCell; LANGUAGE = ObjC; SUPERCLASS = NSTextFieldCell; }, 
        {
            CLASS = SdefClassView; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                commandTable = NSTableView; 
                commands = NSArrayController; 
                elements = NSArrayController; 
                eventTable = NSTableView; 
                events = NSArrayController; 
                properties = NSArrayController; 
                tab = NSTabView; 
            }; 
            SUPERCLASS = SdefViewController; 
        }, 
        {
            CLASS = SdefViewController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                objectController = NSObjectController; 
                ownerController = NSObjectController; 
                sdefView = NSView; 
            }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}