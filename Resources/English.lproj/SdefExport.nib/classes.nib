{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {close = id; include = id; next = id; }; 
            CLASS = SdefExporterController; 
            LANGUAGE = ObjC; 
            OUTLETS = {controller = NSObjectController; includes = NSArrayController; }; 
            SUPERCLASS = NSWindowController; 
        }
    ); 
    IBVersion = 1; 
}