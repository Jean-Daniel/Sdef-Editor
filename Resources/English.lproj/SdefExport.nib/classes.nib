{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {close = id; export = id; include = id; }; 
            CLASS = SdefExporterController; 
            LANGUAGE = ObjC; 
            OUTLETS = {includes = NSArrayController; }; 
            SUPERCLASS = NSWindowController; 
        }
    ); 
    IBVersion = 1; 
}